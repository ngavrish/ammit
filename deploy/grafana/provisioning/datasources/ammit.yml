# The dashboard reads the same sqlite file the server writes and judges by, so
# what is drawn and what was acted on cannot disagree.
apiVersion: 1
datasources:
  - name: ammit
    type: frser-sqlite-datasource
    uid: ammit
    jsonData:
      path: /data/ammit.db
    isDefault: true
