{
 "panels": [
  {
   "kind": "row",
   "title": "The run"
  },
  {
   "kind": "series",
   "title": "Cost per run against limits.usd_per_run",
   "about": "Each line is one run's spend as it accrued. The dashed line is the limit in force at that moment: edit it on the ammit page and the line bends here.",
   "unit": "currencyUSD",
   "height": 8,
   "queries": [
    "SELECT * FROM (SELECT e.at AS time, r.name AS metric, sum(json_extract(e.payload,'$.usd')) OVER (PARTITION BY e.run ORDER BY e.at) AS value FROM events e JOIN runs r ON r.run = e.run WHERE e.run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND e.kind = 'spend' ORDER BY e.at) WHERE time*1000 BETWEEN $__from AND $__to",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'limits.usd_per_run' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'limits.usd_per_run' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "Turns per run against limits.turns_per_run",
   "about": "One per tool call, which is what the pipeline reports as a turn: the heartbeat that proves a session is still moving. Not the number of exchanges with the model.",
   "unit": "short",
   "height": 8,
   "queries": [
    "SELECT * FROM (SELECT e.at AS time, r.name AS metric, count(*) OVER (PARTITION BY e.run ORDER BY e.at) AS value FROM events e JOIN runs r ON r.run = e.run WHERE e.run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND e.kind = 'turn' ORDER BY e.at) WHERE time*1000 BETWEEN $__from AND $__to",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'limits.turns_per_run' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'limits.turns_per_run' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "How long a run has been going, against timeouts.run",
   "about": "A line that reaches the dashed one is a run ammit stopped; the annotation there says what it ran to stop it.",
   "unit": "s",
   "height": 8,
   "queries": [
    "SELECT CAST(e.at/60 AS INTEGER)*60 AS time, r.name AS metric, max(e.at - r.started) AS value FROM events e JOIN runs r ON r.run = e.run WHERE r.started IS NOT NULL GROUP BY 1, 2 ORDER BY 1",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'timeouts.run' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'timeouts.run' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "Idle time piling up, per run",
   "about": "Every gap over two minutes, added up as the run went. A staircase that climbs as fast as the run is a run that is mostly waiting: 377 of 636 minutes, once.",
   "unit": "s",
   "height": 8,
   "queries": [
    "SELECT * FROM (SELECT at AS time, name AS metric, sum(idle) OVER (PARTITION BY run ORDER BY at) AS value FROM ( SELECT e.at AS at, e.run AS run, r.name AS name, CASE WHEN e.at - lag(e.at) OVER (PARTITION BY e.run ORDER BY e.at) > 120 THEN e.at - lag(e.at) OVER (PARTITION BY e.run ORDER BY e.at) ELSE 0 END AS idle FROM events e JOIN runs r ON r.run = e.run WHERE e.run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND e.run IS NOT NULL) ORDER BY 1) WHERE time*1000 BETWEEN $__from AND $__to"
   ]
  },
  {
   "kind": "row",
   "title": "Requests to the model"
  },
  {
   "kind": "series",
   "title": "How long one request takes, by agent, against timeouts.request",
   "about": "One point per wait for the model: ask, and get an answer back. Inside the run this is invisible — a call that never returns has no error and no retry — so it is timed out here, where something can act on it.",
   "unit": "s",
   "height": 8,
   "queries": [
    "SELECT at AS time, coalesce(nullif(agent,''),'?') AS metric, json_extract(payload,'$.seconds') AS value FROM events WHERE at*1000 BETWEEN $__from AND $__to AND kind = 'request_end' ORDER BY at",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'timeouts.request' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'timeouts.request' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "Requests that died, per minute",
   "about": "A request that came back as an error rather than an answer, by agent. A row here and a gap on the chart beside it is a retry; a row here and nothing after it is a session that stopped.",
   "unit": "short",
   "height": 8,
   "queries": [
    "SELECT CAST(at/60 AS INTEGER)*60 AS time, coalesce(nullif(agent,''),'?') AS metric, count(*) AS value FROM events WHERE at*1000 BETWEEN $__from AND $__to AND kind = 'request_end' AND json_extract(payload,'$.ok') = 0 GROUP BY 1, 2 ORDER BY 1"
   ]
  },
  {
   "kind": "series",
   "title": "Request time by phase, against timeouts.request",
   "about": "The same waits, grouped by what the run was doing. A design phase that waits twice as long per request as an implementing one is a prompt problem, not a network problem.",
   "unit": "s",
   "height": 8,
   "queries": [
    "SELECT at AS time, coalesce(nullif(phase,''),'no phase') AS metric, json_extract(payload,'$.seconds') AS value FROM events WHERE at*1000 BETWEEN $__from AND $__to AND kind = 'request_end' ORDER BY at",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'timeouts.request' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'timeouts.request' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "table",
   "title": "Requests that died, in full",
   "about": "What the error was, whose request it was, and how long it had been waiting.",
   "unit": "",
   "height": 8,
   "queries": [
    "SELECT at AS at, run, coalesce(nullif(agent,''),'?') AS agent, coalesce(nullif(phase,''),'') AS phase, ROUND(json_extract(payload,'$.seconds'),1) AS seconds, json_extract(payload,'$.error') AS error FROM events WHERE at*1000 BETWEEN $__from AND $__to AND kind = 'request_end' AND json_extract(payload,'$.ok') = 0 ORDER BY id DESC LIMIT 100"
   ]
  },
  {
   "kind": "row",
   "title": "By phase"
  },
  {
   "kind": "series",
   "title": "Cost by phase",
   "about": "What each phase is costing, as it accrues. \"What is the design costing us\" is this chart and no other.",
   "unit": "currencyUSD",
   "height": 8,
   "queries": [
    "SELECT * FROM (SELECT at AS time, coalesce(nullif(phase,''),'no phase') AS metric, sum(json_extract(payload,'$.usd')) OVER (PARTITION BY coalesce(nullif(phase,''),'no phase') ORDER BY at) AS value FROM events WHERE run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND kind = 'spend' ORDER BY at) WHERE time*1000 BETWEEN $__from AND $__to",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'limits.usd_per_run' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'limits.usd_per_run' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "Turns by phase",
   "about": "One per tool call — runsdb emits a turn from log() when the line is a tool line, so this counts tool calls, not exchanges with the model.",
   "unit": "short",
   "height": 8,
   "queries": [
    "SELECT * FROM (SELECT at AS time, coalesce(nullif(phase,''),'no phase') AS metric, count(*) OVER (PARTITION BY coalesce(nullif(phase,''),'no phase') ORDER BY at) AS value FROM events WHERE run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND kind = 'turn' ORDER BY at) WHERE time*1000 BETWEEN $__from AND $__to"
   ]
  },
  {
   "kind": "series",
   "title": "Phase length against timeouts.phase",
   "about": "One point per finished phase.",
   "unit": "s",
   "height": 8,
   "queries": [
    "SELECT at AS time, coalesce(nullif(phase,''),'?') AS metric, json_extract(payload,'$.seconds') AS value FROM events WHERE at*1000 BETWEEN $__from AND $__to AND kind = 'phase_end' ORDER BY at",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'timeouts.phase' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'timeouts.phase' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "Idle inside a phase, against timeouts.turn",
   "about": "The gap between one thing happening and the next, within a phase. Which phase the waiting happens in is the difference between a slow model and a stuck gate.",
   "unit": "s",
   "height": 8,
   "queries": [
    "SELECT * FROM (SELECT at AS time, coalesce(nullif(phase,''),'no phase') AS metric, at - lag(at) OVER (PARTITION BY run, coalesce(phase,'') ORDER BY at) AS value FROM events WHERE run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND run IS NOT NULL AND kind IN ('turn','log','session_start','phase_start') ORDER BY at) WHERE time*1000 BETWEEN $__from AND $__to",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'timeouts.turn' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'timeouts.turn' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "Memory while each phase ran, against limits.memory_mb",
   "about": "Every sampled container added up, labelled with the phase that was open when the reading was taken. It is a claim about time, not about blame: several branches share a worker.",
   "unit": "mbytes",
   "height": 8,
   "queries": [
    "SELECT t AS time, phase AS metric, mb AS value FROM ( SELECT s.at AS t, coalesce((SELECT p.phase FROM events p WHERE p.kind = 'phase_start' AND ifnull(p.phase,'') <> '' AND p.at <= s.at ORDER BY p.at DESC LIMIT 1),'no phase') AS phase, sum(json_extract(s.payload,'$.memory_mb')) AS mb FROM events s WHERE s.at*1000 BETWEEN $__from AND $__to AND s.kind = 'sample' GROUP BY s.at) ORDER BY 1",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'limits.memory_mb' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'limits.memory_mb' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "Time in a phase, minute by minute",
   "about": "How long the open phase has been open. A climb that does not reset is a phase nothing is ending.",
   "unit": "s",
   "height": 8,
   "queries": [
    "SELECT CAST(e.at/60 AS INTEGER)*60 AS time, coalesce(nullif(e.phase,''),'no phase') AS metric, max(e.at - (SELECT p.at FROM events p WHERE p.kind = 'phase_start' AND p.run = e.run AND p.phase = e.phase AND p.at <= e.at ORDER BY p.at DESC LIMIT 1)) AS value FROM events e WHERE e.at*1000 BETWEEN $__from AND $__to AND e.run IS NOT NULL AND ifnull(e.phase,'') <> '' GROUP BY 1, 2 ORDER BY 1",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'timeouts.phase' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'timeouts.phase' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "row",
   "title": "By agent session"
  },
  {
   "kind": "series",
   "title": "Cost by agent",
   "about": "Which agent is spending the money.",
   "unit": "currencyUSD",
   "height": 8,
   "queries": [
    "SELECT * FROM (SELECT at AS time, coalesce(nullif(agent,''),'?') AS metric, sum(json_extract(payload,'$.usd')) OVER (PARTITION BY coalesce(nullif(agent,''),'?') ORDER BY at) AS value FROM events WHERE run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND kind = 'spend' ORDER BY at) WHERE time*1000 BETWEEN $__from AND $__to",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'limits.usd_per_run' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'limits.usd_per_run' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "What one turn carried, against limits.turn_tokens",
   "about": "Every turn as it was sent — the system prompt, whatever was inlined into it, and the conversation so far. The per-session average below is a summary that arrives once the session is over; this is the number while the next turn has not been paid for yet.",
   "unit": "short",
   "height": 8,
   "queries": [
    "SELECT at*1000 AS time, coalesce(nullif(agent,''),'?') AS metric, json_extract(payload,'$.context') AS value FROM events WHERE kind = 'turn' AND coalesce(json_extract(payload,'$.context'),0) > 0 ORDER BY at",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'limits.turn_tokens' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'limits.turn_tokens' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "Output per turn, by agent",
   "about": "What the model actually wrote. Read this beside the chart on the left: three hundred tokens read for every one written is a prompt problem, not a model having a hard think.",
   "unit": "short",
   "height": 8,
   "queries": [
    "SELECT at*1000 AS time, coalesce(nullif(agent,''),'?') AS metric, json_extract(payload,'$.tokens_out') AS value FROM events WHERE kind = 'turn' AND coalesce(json_extract(payload,'$.tokens_out'),0) > 0 ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "Turns by agent",
   "about": "Which agent is taking the turns.",
   "unit": "short",
   "height": 8,
   "queries": [
    "SELECT * FROM (SELECT at AS time, coalesce(nullif(agent,''),'?') AS metric, count(*) OVER (PARTITION BY coalesce(nullif(agent,''),'?') ORDER BY at) AS value FROM events WHERE run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND kind = 'turn' ORDER BY at) WHERE time*1000 BETWEEN $__from AND $__to"
   ]
  },
  {
   "kind": "series",
   "title": "Session length against timeouts.session",
   "about": "One point per finished session, by agent.",
   "unit": "s",
   "height": 8,
   "queries": [
    "SELECT at AS time, coalesce(nullif(agent,''),'?') AS metric, json_extract(payload,'$.seconds') AS value FROM events WHERE at*1000 BETWEEN $__from AND $__to AND kind = 'session_end' ORDER BY at",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'timeouts.session' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'timeouts.session' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "Silence between turns, against timeouts.turn",
   "about": "The gap between one turn and the next, per agent.",
   "unit": "s",
   "height": 8,
   "queries": [
    "SELECT * FROM (SELECT at AS time, coalesce(nullif(agent,''),'?') AS metric, at - lag(at) OVER (PARTITION BY run, coalesce(agent,'') ORDER BY at) AS value FROM events WHERE run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND kind IN ('turn','session_start') AND run IS NOT NULL ORDER BY at) WHERE time*1000 BETWEEN $__from AND $__to",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'timeouts.turn' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'timeouts.turn' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "Memory while each agent ran, against limits.memory_mb",
   "about": "The same readings, labelled with the session that was open when they were taken.",
   "unit": "mbytes",
   "height": 8,
   "queries": [
    "SELECT t AS time, agent AS metric, mb AS value FROM ( SELECT s.at AS t, coalesce((SELECT a.agent FROM events a WHERE a.kind = 'session_start' AND ifnull(a.agent,'') <> '' AND a.at <= s.at ORDER BY a.at DESC LIMIT 1),'nobody') AS agent, sum(json_extract(s.payload,'$.memory_mb')) AS mb FROM events s WHERE s.at*1000 BETWEEN $__from AND $__to AND s.kind = 'sample' GROUP BY s.at) ORDER BY 1",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'limits.memory_mb' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'limits.memory_mb' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "Time in a session, minute by minute",
   "about": "How long the open session has been open, by agent.",
   "unit": "s",
   "height": 8,
   "queries": [
    "SELECT CAST(e.at/60 AS INTEGER)*60 AS time, coalesce(nullif(e.agent,''),'?') AS metric, max(e.at - (SELECT a.at FROM events a WHERE a.kind = 'session_start' AND a.run = e.run AND a.agent = e.agent AND a.at <= e.at ORDER BY a.at DESC LIMIT 1)) AS value FROM events e WHERE e.at*1000 BETWEEN $__from AND $__to AND e.run IS NOT NULL AND ifnull(e.agent,'') <> '' GROUP BY 1, 2 ORDER BY 1",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'timeouts.session' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'timeouts.session' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "row",
   "title": "Tokens"
  },
  {
   "kind": "series",
   "title": "Tokens out, per run",
   "about": "Generated tokens as they accrued. This is the number that moves the bill; cache read below is charged at a fraction of it.",
   "unit": "short",
   "height": 8,
   "queries": [
    "SELECT * FROM (SELECT at AS time, (SELECT r.name FROM runs r WHERE r.run = events.run) AS metric, sum(json_extract(payload,'$.tokens_out')) OVER (PARTITION BY (SELECT r.name FROM runs r WHERE r.run = events.run) ORDER BY at) AS value FROM events WHERE run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND kind = 'spend' ORDER BY at) WHERE time*1000 BETWEEN $__from AND $__to"
   ]
  },
  {
   "kind": "series",
   "title": "Tokens out, by phase",
   "about": "Which phase is generating the volume.",
   "unit": "short",
   "height": 8,
   "queries": [
    "SELECT * FROM (SELECT at AS time, coalesce(nullif(phase,''),'no phase') AS metric, sum(json_extract(payload,'$.tokens_out')) OVER (PARTITION BY coalesce(nullif(phase,''),'no phase') ORDER BY at) AS value FROM events WHERE run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND kind = 'spend' ORDER BY at) WHERE time*1000 BETWEEN $__from AND $__to"
   ]
  },
  {
   "kind": "series",
   "title": "Tokens out, by agent",
   "about": "And which agent inside it.",
   "unit": "short",
   "height": 8,
   "queries": [
    "SELECT * FROM (SELECT at AS time, coalesce(nullif(agent,''),'?') AS metric, sum(json_extract(payload,'$.tokens_out')) OVER (PARTITION BY coalesce(nullif(agent,''),'?') ORDER BY at) AS value FROM events WHERE run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND kind = 'spend' ORDER BY at) WHERE time*1000 BETWEEN $__from AND $__to"
   ]
  },
  {
   "kind": "series",
   "title": "Cache read, per run",
   "about": "Context read back rather than re-sent. Large is good here: it is the same prompt not being paid for twice.",
   "unit": "short",
   "height": 8,
   "queries": [
    "SELECT * FROM (SELECT at AS time, (SELECT r.name FROM runs r WHERE r.run = events.run) AS metric, sum(json_extract(payload,'$.cache_read')) OVER (PARTITION BY (SELECT r.name FROM runs r WHERE r.run = events.run) ORDER BY at) AS value FROM events WHERE run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND kind = 'spend' ORDER BY at) WHERE time*1000 BETWEEN $__from AND $__to"
   ]
  },
  {
   "kind": "series",
   "title": "Cache written, per run",
   "about": "Context put into the cache. Written once, read many times — a run where this keeps climbing is a run whose prompt will not settle.",
   "unit": "short",
   "height": 8,
   "queries": [
    "SELECT * FROM (SELECT at AS time, (SELECT r.name FROM runs r WHERE r.run = events.run) AS metric, sum(json_extract(payload,'$.cache_write')) OVER (PARTITION BY (SELECT r.name FROM runs r WHERE r.run = events.run) ORDER BY at) AS value FROM events WHERE run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND kind = 'spend' ORDER BY at) WHERE time*1000 BETWEEN $__from AND $__to"
   ]
  },
  {
   "kind": "table",
   "title": "Tokens and what they cost",
   "about": "Every run in the window: what it generated, what it read back, and the bill.",
   "unit": "",
   "height": 9,
   "queries": [
    "SELECT r.started AS started, r.name AS run, coalesce(r.verdict,'running') AS verdict, CAST(sum(json_extract(e.payload,'$.tokens_out')) AS INTEGER) AS tokens_out, CAST(sum(json_extract(e.payload,'$.cache_read')) AS INTEGER) AS cache_read, CAST(sum(json_extract(e.payload,'$.cache_write')) AS INTEGER) AS cache_write, ROUND(sum(json_extract(e.payload,'$.usd')), 2) AS usd FROM events e JOIN runs r ON r.run = e.run WHERE e.kind = 'spend' AND e.run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) GROUP BY e.run ORDER BY r.started DESC LIMIT 50"
   ]
  },
  {
   "kind": "row",
   "title": "The machine"
  },
  {
   "kind": "series",
   "title": "Memory by container against limits.memory_mb",
   "about": "Read from outside every sample.every seconds. A worker the kernel kills leaves no evidence of its own: the process that would have said so is the one that died.",
   "unit": "mbytes",
   "height": 8,
   "queries": [
    "SELECT at AS time, json_extract(payload,'$.container') AS metric, json_extract(payload,'$.memory_mb') AS value FROM events WHERE at*1000 BETWEEN $__from AND $__to AND kind = 'sample' ORDER BY at",
    "SELECT at AS time, name AS metric, value FROM limits WHERE name = 'limits.memory_mb' AND at*1000 <= $__to AND (at*1000 >= $__from OR at = (SELECT max(at) FROM limits WHERE name = 'limits.memory_mb' AND at*1000 < $__from)) ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "CPU by container",
   "about": "Percent of one core, as the container client reports it.",
   "unit": "percent",
   "height": 8,
   "queries": [
    "SELECT at AS time, json_extract(payload,'$.container') AS metric, json_extract(payload,'$.cpu_pct') AS value FROM events WHERE at*1000 BETWEEN $__from AND $__to AND kind = 'sample' ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "Processes by container",
   "about": "A count that climbs and never falls is something not being reaped — the browser runs this pipeline leaves behind, most often.",
   "unit": "short",
   "height": 8,
   "queries": [
    "SELECT at AS time, json_extract(payload,'$.container') AS metric, json_extract(payload,'$.pids') AS value FROM events WHERE at*1000 BETWEEN $__from AND $__to AND kind = 'sample' ORDER BY at"
   ]
  },
  {
   "kind": "states",
   "title": "Phases and branches",
   "about": "What each branch was doing, minute by minute.",
   "unit": "",
   "height": 8,
   "queries": [
    "SELECT time, run, phase FROM ( SELECT CAST(e.at/60 AS INTEGER)*60 AS time, max('#' || n.seq) AS run, max(coalesce(nullif(e.phase,''), e.agent, '?')) AS phase FROM events e LEFT JOIN (SELECT run, dense_rank() OVER (ORDER BY started) AS seq FROM runs) n ON n.run = e.run WHERE e.at*1000 BETWEEN $__from AND $__to AND e.kind IN ('turn','log','session_start') GROUP BY 1) ORDER BY time"
   ]
  },
  {
   "kind": "row",
   "title": "Gates and repair"
  },
  {
   "kind": "series",
   "title": "Findings per gate, each time it ran",
   "about": "How much each check refuses, round by round. A gate that never finds anything is a tollbooth; one whose findings do not fall between rounds is asking for something the repair cannot give.",
   "unit": "short",
   "height": 8,
   "queries": [
    "SELECT at*1000 AS time, phase AS metric, findings AS value FROM gates ORDER BY at"
   ]
  },
  {
   "kind": "series",
   "title": "How long a round takes, by gate",
   "about": "The check plus the repair it caused. A loop that costs more than the fault is a loop worth removing.",
   "unit": "s",
   "height": 8,
   "queries": [
    "SELECT at*1000 AS time, phase AS metric, seconds AS value FROM gates WHERE seconds > 0 ORDER BY at"
   ]
  },
  {
   "kind": "table",
   "title": "Rounds to green, per gate",
   "about": "How many times each check had to run before it stopped refusing, and what the rounds cost. One round is a check doing its job; three is a repair that cannot hear what is being asked.",
   "unit": "",
   "height": 9,
   "queries": [
    "SELECT phase, coalesce(nullif(branch,''),'—') AS branch, max(round) AS rounds, sum(findings) AS findings_total, ROUND(sum(seconds)/60.0,1) AS minutes, max(CASE WHEN verdict='green' THEN 'reached green' ELSE verdict END) AS ended FROM gates GROUP BY phase, branch ORDER BY minutes DESC LIMIT 60"
   ]
  },
  {
   "kind": "row",
   "title": "Where the time went"
  },
  {
   "kind": "table",
   "title": "Busy and idle, per run",
   "about": "Elapsed against the part of it where nothing happened for more than two minutes.",
   "unit": "",
   "height": 9,
   "queries": [
    "SELECT r.started AS started, r.finished AS finished, r.name AS run, coalesce(r.verdict,'running') AS verdict, CAST((coalesce(r.finished, strftime('%s','now')) - r.started)/60 AS INTEGER) AS elapsed_min, CAST(((coalesce(r.finished, strftime('%s','now')) - r.started) - coalesce(g.idle,0))/60 AS INTEGER) AS busy_min, CAST(coalesce(g.idle,0)/60 AS INTEGER) AS idle_min, ROUND(100.0*coalesce(g.idle,0) / max(1.0, coalesce(r.finished, strftime('%s','now')) - r.started), 1) AS idle_pct, ROUND(coalesce(r.usd,0),2) AS usd FROM runs r LEFT JOIN ( SELECT run, sum(gap) AS idle FROM ( SELECT run, CASE WHEN at - lag(at) OVER (PARTITION BY run ORDER BY at) > 120 THEN at - lag(at) OVER (PARTITION BY run ORDER BY at) ELSE 0 END AS gap FROM events WHERE run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND run IS NOT NULL) GROUP BY run) g ON g.run = r.run ORDER BY r.started DESC LIMIT 50"
   ]
  },
  {
   "kind": "table",
   "title": "The longest gaps",
   "about": "Every stretch over five minutes where nothing was reported, newest first. What was open when it started is the first place to look.",
   "unit": "",
   "height": 9,
   "queries": [
    "SELECT * FROM (SELECT at AS at, run, coalesce(nullif(phase,''),'') AS phase, coalesce(nullif(agent,''),'') AS agent, CAST(gap/60 AS INTEGER) AS minutes FROM (SELECT at, run, phase, agent, at - lag(at) OVER (PARTITION BY run ORDER BY at) AS gap FROM events WHERE run NOT IN (SELECT run FROM runs WHERE started*1000 > $__to OR (finished IS NOT NULL AND finished*1000 < $__from)) AND run IS NOT NULL) WHERE gap > 300 ORDER BY at DESC LIMIT 50) WHERE at*1000 BETWEEN $__from AND $__to"
   ]
  },
  {
   "kind": "table",
   "title": "What ammit did",
   "about": "Every limit crossed, what it did about it, and what the command said back.",
   "unit": "",
   "height": 9,
   "queries": [
    "SELECT at AS at, scope, coalesce(subject,'') AS subject, rule, threshold, observed, action, coalesce(outcome,'') AS outcome FROM judgements ORDER BY id DESC LIMIT 200"
   ]
  },
  {
   "kind": "table",
   "title": "Runs",
   "about": "",
   "unit": "",
   "height": 9,
   "queries": [
    "SELECT started AS started, finished AS finished, name, coalesce(verdict,'running') AS verdict, CAST((coalesce(finished, strftime('%s','now')) - started)/60 AS INTEGER) AS minutes, ROUND(coalesce(usd,0),2) AS usd, coalesce(turns,0) AS turns, coalesce(summary,'') AS summary FROM runs ORDER BY started DESC LIMIT 50"
   ]
  },
  {
   "kind": "table",
   "title": "Limits, as they stand",
   "about": "What every chart above is drawn against, and when it last changed.",
   "unit": "",
   "height": 9,
   "queries": [
    "SELECT name, value, at AS changed FROM limits WHERE id IN (SELECT max(id) FROM limits GROUP BY name) ORDER BY name"
   ]
  },
  {
   "kind": "row",
   "title": "What was done twice"
  },
  {
   "kind": "table",
   "title": "The same call, again",
   "about": "One row per distinct call that ran more than once in the window. This is the question the calls table was added for: a branch ran the same search for step phrases eleven times, and nobody knew until somebody counted by hand.",
   "unit": "",
   "height": 9,
   "queries": [
    "SELECT agent, branch, kind, count(*) AS times, signature FROM calls WHERE at*1000 BETWEEN $__from AND $__to GROUP BY agent, branch, signature HAVING times > 1 ORDER BY times DESC LIMIT 60"
   ]
  },
  {
   "kind": "table",
   "title": "The same file, however it was reached",
   "about": "Grouped by what was touched rather than how it was touched. Read, then cat, then three sed windows is five calls and one file — which is how a single step module came to be opened fifteen times in fourteen different ways.",
   "unit": "",
   "height": 9,
   "queries": [
    "SELECT agent, branch, target, group_concat(DISTINCT kind) AS kinds, count(*) AS times, count(DISTINCT signature) AS ways FROM calls WHERE at*1000 BETWEEN $__from AND $__to AND ifnull(target,'') <> '' GROUP BY agent, branch, target HAVING times > 1 ORDER BY times DESC LIMIT 60"
   ]
  },
  {
   "kind": "table",
   "title": "What each phase spent its calls on",
   "about": "Every call of the window by kind. Writing and running tests are the work; everything else is what it cost to get there.",
   "unit": "",
   "height": 9,
   "queries": [
    "SELECT phase, kind, count(*) AS times FROM calls WHERE at*1000 BETWEEN $__from AND $__to GROUP BY phase, kind ORDER BY times DESC LIMIT 80"
   ]
  }
 ]
}
